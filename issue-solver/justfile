#!/usr/bin/env just --justfile

# ✅ Run all tests
test:
  uv run pytest

# 🧹 Lint the code
lint:
    uv run ruff check

# 🎨 Format the code
format:
    uv run ruff format

set dotenv-load
# 🚀 Start the API
api-start:
    uv run fastapi dev src/issue_solver/webapi/main.py